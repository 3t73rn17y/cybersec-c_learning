.phase { margin-bottom: 2rem; } .phase-header { display: flex; align-items: center; gap: 10px; margin-bottom: 1rem; padding-bottom: 0.5rem; border-bottom: 1px solid var(--color-border-tertiary); } .phase-badge { font-size: 11px; font-weight: 500; padding: 3px 10px; border-radius: 12px; white-space: nowrap; } .phase-title { font-size: 16px; font-weight: 500; color: var(--color-text-primary); margin: 0; } .phase-sub { font-size: 13px; color: var(--color-text-secondary); margin: 0 0 0 4px; } .projects { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 12px; } .card { background: var(--color-background-secondary); border: 1px solid var(--color-border-tertiary); border-radius: 10px; padding: 14px 16px; cursor: pointer; transition: border-color 0.15s, box-shadow 0.15s; } .card:hover { border-color: var(--color-border-secondary); } .card-top { display: flex; align-items: flex-start; justify-content: space-between; gap: 8px; margin-bottom: 6px; } .card-title { font-size: 14px; font-weight: 500; color: var(--color-text-primary); line-height: 1.35; } .tag { font-size: 10px; font-weight: 500; padding: 2px 7px; border-radius: 8px; white-space: nowrap; flex-shrink: 0; margin-top: 2px; } .card-desc { font-size: 12px; color: var(--color-text-secondary); line-height: 1.55; margin-bottom: 8px; } .skills-row { display: flex; flex-wrap: wrap; gap: 4px; } .skill { font-size: 10px; color: var(--color-text-tertiary); background: var(--color-background-primary); border: 1px solid var(--color-border-tertiary); border-radius: 6px; padding: 1px 7px; font-family: var(--font-mono); } .modal-overlay { position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,0.5); display: flex; align-items: center; justify-content: center; z-index: 10; padding: 16px; display: none; } .modal { background: var(--color-background-primary); border: 1px solid var(--color-border-secondary); border-radius: 12px; padding: 20px; max-width: 480px; width: 100%; } .modal h3 { font-size: 15px; font-weight: 500; color: var(--color-text-primary); margin: 0 0 8px; } .modal p { font-size: 13px; color: var(--color-text-secondary); line-height: 1.6; margin: 0 0 12px; } .modal-skills { margin-bottom: 14px; } .modal-skills h4 { font-size: 11px; font-weight: 500; color: var(--color-text-tertiary); text-transform: uppercase; letter-spacing: 0.05em; margin: 0 0 6px; } .modal-skills ul { margin: 0; padding-left: 16px; } .modal-skills li { font-size: 13px; color: var(--color-text-secondary); margin-bottom: 3px; } .modal-actions { display: flex; gap: 8px; } .btn-primary { background: var(--color-text-primary); color: var(--color-background-primary); border: none; border-radius: 7px; padding: 8px 14px; font-size: 13px; cursor: pointer; font-weight: 500; } .btn-secondary { background: transparent; color: var(--color-text-secondary); border: 1px solid var(--color-border-secondary); border-radius: 7px; padding: 8px 14px; font-size: 13px; cursor: pointer; } .wrap { padding: 1rem 0; position: relative; min-height: 200px; } /\* color themes \*/ .bg-purple { background: #EEEDFE; color: #3C3489; } .bg-teal { background: #E1F5EE; color: #0F6E56; } .bg-amber { background: #FAEEDA; color: #854F0B; } .bg-coral { background: #FAECE7; color: #993C1D; } .tag-purple { background: #EEEDFE; color: #3C3489; } .tag-teal { background: #E1F5EE; color: #0F6E56; } .tag-amber { background: #FAEEDA; color: #854F0B; } .tag-coral { background: #FAECE7; color: #993C1D; } @media (prefers-color-scheme: dark) { .bg-purple { background: #26215C; color: #AFA9EC; } .bg-teal { background: #04342C; color: #5DCAA5; } .bg-amber { background: #412402; color: #EF9F27; } .bg-coral { background: #4A1B0C; color: #F0997B; } .tag-purple { background: #26215C; color: #AFA9EC; } .tag-teal { background: #04342C; color: #5DCAA5; } .tag-amber { background: #412402; color: #EF9F27; } .tag-coral { background: #4A1B0C; color: #F0997B; } }

const phases = \[ { label: "Phase 1", title: "C fundamentals + OS & networking basics", badge: "Beginner", color: "teal", projects: \[ { title: "Caesar cipher CLI tool", tag: "C + Cryptography", tagColor: "teal", desc: "Build a command-line program that encrypts and decrypts text using the Caesar cipher. Teaches CLI argument parsing, ASCII math, and basic file I/O.", learns: \["Variables, loops, functions in C", "stdin/stdout and argc/argv", "Basic cryptography concepts", "How substitution ciphers work"\], skills: \["pointers", "argv", "stdio.h", "ASCII"\] }, { title: "Port scanner", tag: "C + Networking", tagColor: "teal", desc: "Write a simple TCP port scanner that attempts to connect to a range of ports on a target host and reports which are open.", learns: \["BSD sockets API (socket, connect, close)", "TCP/IP fundamentals", "What open/closed/filtered ports mean", "Basic network recon concepts"\], skills: \["sys/socket.h", "connect()", "TCP/IP", "errno"\] }, { title: "File integrity checker", tag: "C + Security", tagColor: "teal", desc: "Implement MD5 or SHA-256 from scratch (or using OpenSSL) to hash files and detect tampering. Save a baseline and re-check on demand.", learns: \["Hashing and hash functions", "Why integrity matters (MITRE concepts)", "File I/O in C", "Using a C library (OpenSSL)"\], skills: \["fread()", "OpenSSL", "SHA-256", "hex output"\] }, { title: "Syslog parser", tag: "C + Monitoring", tagColor: "teal", desc: "Read /var/log/syslog (or a log file), parse lines with sscanf/regex, and flag lines matching suspicious patterns like failed SSH logins.", learns: \["String parsing in C (sscanf, strtok)", "Log analysis fundamentals", "Pattern matching basics", "SIEM concepts"\], skills: \["sscanf", "regex.h", "fgets", "logging"\] } \] }, { label: "Phase 2", title: "Memory, processes & defensive programming", badge: "Intermediate", color: "purple", projects: \[ { title: "Buffer overflow demo lab", tag: "C + Exploitation", tagColor: "purple", desc: "Write a deliberately vulnerable C program, then write an exploit for it. Then patch it. Understanding the attack makes you a better defender.", learns: \["Stack layout, stack frames, return addresses", "How buffer overflows work", "ASLR, stack canaries, NX bit", "Secure coding in C"\], skills: \["gdb", "stack frames", "gets() vs fgets()", "ASLR"\] }, { title: "Password manager (local)", tag: "C + Cryptography", tagColor: "purple", desc: "Store passwords encrypted on disk using AES-256 via OpenSSL. Implement a master password with PBKDF2 key derivation.", learns: \["Symmetric encryption (AES)", "Key derivation functions", "Secure memory handling (zeroing secrets)", "Why storing plaintext is dangerous"\], skills: \["AES-256", "PBKDF2", "OpenSSL EVP", "memset\_s"\] }, { title: "Packet sniffer", tag: "C + Networking", tagColor: "purple", desc: "Use libpcap to capture and dissect network packets — parse Ethernet, IP, TCP/UDP headers and print a tcpdump-like summary.", learns: \["Raw packet structure (Ethernet/IP/TCP headers)", "How Wireshark/tcpdump work internally", "OSI model hands-on", "Promiscuous mode"\], skills: \["libpcap", "struct unpacking", "ntohl/ntohs", "raw sockets"\] }, { title: "Intrusion detection system (IDS) prototype", tag: "C + Detection", tagColor: "purple", desc: "Build on the packet sniffer: detect port scans (many SYNs from one IP), large ICMP payloads, and log alerts to a file.", learns: \["Signature-based detection logic", "IDS/IPS concepts", "Rate limiting and thresholds", "Event correlation basics"\], skills: \["pcap\_loop()", "hash maps in C", "time.h", "alert logging"\] } \] }, { label: "Phase 3", title: "Web, auth & penetration testing concepts", badge: "Intermediate", color: "amber", projects: \[ { title: "HTTP server with auth", tag: "C + Web Security", tagColor: "amber", desc: "Write a minimal HTTP/1.1 server from scratch. Add basic auth with bcrypt-hashed passwords. Test it with your own tools for common vulnerabilities.", learns: \["HTTP request/response parsing", "Authentication mechanisms", "Password hashing (bcrypt)", "Why HTTP headers matter for security"\], skills: \["recv()/send()", "HTTP parsing", "bcrypt", "pthreads"\] }, { title: "SQL injection demo & mitigations", tag: "C + Web Security", tagColor: "amber", desc: "Write a C program with SQLite that has a SQL injection vulnerability. Exploit it, then fix it with prepared statements.", learns: \["How SQL injection works", "Parameterized queries", "Input validation principles", "OWASP Top 10 hands-on"\], skills: \["sqlite3.h", "prepared stmts", "input sanitization", "OWASP"\] }, { title: "TLS/SSL handshake tracer", tag: "C + Cryptography", tagColor: "amber", desc: "Use OpenSSL to make a TLS connection to a server and print every step of the handshake: cipher negotiation, certificate chain, key exchange.", learns: \["How TLS 1.3 works", "Public key infrastructure (PKI)", "Certificate validation", "Why HTTPS matters"\], skills: \["SSL\_connect()", "X.509 certs", "cipher suites", "MITM concepts"\] }, { title: "Vulnerability scanner (CVE lookup)", tag: "C + Recon", tagColor: "amber", desc: "Write a tool that banner-grabs a service (SSH, FTP, HTTP), extracts the version, and queries the NVD API to list known CVEs for that version.", learns: \["Banner grabbing", "CVE/NVD databases", "Recon methodology", "HTTP client requests from C"\], skills: \["recv() banners", "JSON parsing", "NVD API", "version matching"\] } \] }, { label: "Phase 4", title: "Advanced — exploitation, forensics & defense", badge: "Advanced", color: "coral", projects: \[ { title: "Rootkit detector", tag: "C + Forensics", tagColor: "coral", desc: "Compare /proc listings with the output of system calls directly to detect discrepancies that rootkits use to hide processes.", learns: \["Linux /proc filesystem", "How rootkits hide", "Syscall-level programming", "Forensic methodology"\], skills: \["/proc/", "ptrace", "readdir()", "syscalls"\] }, { title: "Shellcode loader (CTF lab)", tag: "C + Exploitation", tagColor: "coral", desc: "In a safe VM, write a program that allocates executable memory with mmap and runs shellcode from a file. Explore mitigations like W^X.", learns: \["mmap and memory permissions", "Shellcode and payloads", "W^X / DEP protection", "Exploit mitigations"\], skills: \["mmap(PROT\_EXEC)", "mprotect", "shellcode", "W^X"\] }, { title: "Network honeypot", tag: "C + Defense", tagColor: "coral", desc: "Bind to common ports (21, 22, 80) and act like real services long enough to log attacker IPs, credentials tried, and commands entered.", learns: \["Deception-based defense", "Attacker behavior patterns", "Threat intelligence collection", "Event logging and alerting"\], skills: \["epoll/select", "fake protocol resp.", "GeoIP lookup", "SIEM export"\] }, { title: "Binary analysis tool", tag: "C + Reverse Eng.", tagColor: "coral", desc: "Parse ELF binaries yourself — read headers, list sections, find symbols, and detect common compiler protections (PIE, RELRO, stack canary).", learns: \["ELF binary format", "Reverse engineering basics", "Compiler security features", "What tools like checksec do internally"\], skills: \["elf.h", "mmap file", "ELF headers", "PIE/RELRO/canary"\] } \] } \]; function renderPhases() { const el = document.getElementById('content'); el.innerHTML = phases.map(phase => \` <div class="phase"> <div class="phase-header"> <span class="phase-badge bg-${phase.color}">${phase.label}</span> <span class="phase-title">${phase.title}</span> </div> <div class="projects"> ${phase.projects.map((p, i) => \` <div class="card" onclick="openModal('${phase.label}', ${i})"> <div class="card-top"> <span class="card-title">${p.title}</span> <span class="tag tag-${p.tagColor}">${p.tag}</span> </div> <div class="card-desc">${p.desc}</div> <div class="skills-row">${p.skills.map(s => \`<span class="skill">${s}</span>\`).join('')}</div> </div> \`).join('')} </div> </div> \`).join(''); } function openModal(phaseLabel, idx) { const phase = phases.find(p => p.label === phaseLabel); const p = phase.projects\[idx\]; const modal = document.getElementById('modal'); modal.innerHTML = \` <h3>${p.title}</h3> <p>${p.desc}</p> <div class="modal-skills"> <h4>What you'll learn</h4> <ul>${p.learns.map(l => \`<li>${l}</li>\`).join('')}</ul> </div> <div class="modal-actions"> <button class="btn-primary" onclick="sendPrompt('Give me a step by step guide to build the \\\\'${p.title}\\\\' project in C for learning cybersecurity')">Build this project</button> <button class="btn-secondary" onclick="closeModal()">Close</button> </div> \`; document.getElementById('modal-overlay').style.display = 'flex'; } function closeModal() { document.getElementById('modal-overlay').style.display = 'none'; } document.getElementById('modal-overlay').addEventListener('click', e => { if (e.target === document.getElementById('modal-overlay')) closeModal(); }); renderPhases();
